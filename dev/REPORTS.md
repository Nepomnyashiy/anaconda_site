# Отчёты по задачам

- T-02 — завершено: настроен CI/CD для сборки и публикации Docker-образов `web` и `api` в GHCR с тегами `latest`, `sha-*` и `semver` по релизному тегу.
- T-02.1 — завершено: добавлен контейнер `pgadmin` в `infra/compose.dev.yml`, добавлены переменные `PGADMIN_*` в `.env`, обновлена документация по деплою и архитектуре.
