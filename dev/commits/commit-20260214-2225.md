# Отчёт по коммиту [HOTFIX] CI: исправлен синтаксис workflow deploy

Дата: 2026-02-14 22:25

Что сделано:
- Исправлен YAML-синтаксис в `.github/workflows/blank.yml` (ошибка на строке 54).
- Переписан блок генерации `ansible/inventory/hosts.ini` в шаге CI так, чтобы он использовал безопасный `printf` с `\n` без разрывов строк, ломающих YAML.
- Сохранена существующая логика: проверка секретов, подготовка SSH, fail-fast проверки и запуск `ansible-playbook`.

Проверки:
- `python - <<'PY' ... yaml.safe_load('.github/workflows/blank.yml') ... PY` — успешно, YAML валиден.

Результат:
- Workflow снова корректно парсится GitHub Actions и может быть выполнен.
