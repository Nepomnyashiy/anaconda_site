# Отчёт по коммиту [HOTFIX] Web: добавлен public + минимальные env-переменные для compose

Дата: 2026-02-14 22:17

Что сделано:
- Добавлена директория `web/public/` с файлом `web/public/.gitkeep`, чтобы сборка Docker не падала на шаге копирования `/app/public`.
- Создан файл `.env.example` с минимально необходимыми переменными для `docker compose`.
- Обновлён `docs/DEPLOY.md`: добавлен раздел с обязательными переменными окружения и примером заполнения.

Проверки:
- `docker compose -f infra/compose.dev.yml build --no-cache` — не выполнено в окружении: отсутствует команда `docker`.
- `docker compose -f infra/compose.dev.yml up -d` — не выполнено в окружении: отсутствует команда `docker`.
- `docker ps` — не выполнено в окружении: отсутствует команда `docker`.
- `curl -I http://localhost` — сервис недоступен, т.к. контейнеры не запускались.
- `curl -s http://localhost/api/health || true` — команда выполнена, ответ пустой.

Результат:
- Подготовлен минимальный hotfix для устранения проблемы с отсутствием `public` и предупреждений по обязательным переменным окружения в compose.
