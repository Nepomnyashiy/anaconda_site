# This file contains DEV values only. Production secrets are managed via Ansible Vault.
PROJECT_NAME=anaconda-www
ENV=dev
DOMAIN=localhost

WEB_PORT=3000
API_PORT=8000
NGINX_PORT=80

POSTGRES_DB=anaconda
POSTGRES_USER=anaconda
POSTGRES_PASSWORD=anaconda

POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# -------------------------
# pgAdmin (dev/demo only)
# -------------------------

# Email для входа в pgAdmin
PGADMIN_EMAIL=admin@localhost
# Пароль для входа в pgAdmin (только dev)
PGADMIN_PASSWORD=anaconda
# Порт для доступа к pgAdmin
PGADMIN_PORT=5050

REDIS_HOST=redis
REDIS_PORT=6379

AI_PROVIDER=demo
OPENROUTER_API_KEY=

# -------------------------
# Ansible deploy variables
# -------------------------
# Обязательные переменные из ansible/playbooks/site.yml.
# В CI/production значения должны управляться через inventory/group_vars/Vault.
DEPLOY_DIR=/opt/www-stack
COMPOSE_FILE_NAME=compose.prod.yml
ENV_FILE_NAME=.env

# -------------------------
# CI deploy secrets mapping
# -------------------------
# Эти переменные соответствуют GitHub Secrets, используемым workflow деплоя.
# Не храните реальные секреты в репозитории.
DEPLOY_HOST=31.59.106.120
DEPLOY_USER=deploy
DEPLOY_PORT=22
DEPLOY_SSH_KEY=
